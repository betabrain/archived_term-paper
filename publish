make -j 4
targetpath="pdfs/`date +"main_%Y-%m-%d_%H-%M.pdf"`"
cp -v main.pdf "$targetpath"
git add "$targetpath"
git commit -m "newly rendered pdf: $targetpath"
evince "$targetpath" &>/dev/null &
make clean

